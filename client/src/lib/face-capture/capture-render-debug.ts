// ============================================================
// Bascules de rendu capture (overlay live + aplatis admin).
// Le maillage ovale WebGL « blanc debug » peut être réactivé sans retoucher les math.
// ============================================================

/** À `true` : réaffiche ovale + filaire intérieur (WebGL) en plus des repères couleur sur les poses concernées. */
export const DEBUG_CAPTURE_WHITE_FACE_MESH = false;
