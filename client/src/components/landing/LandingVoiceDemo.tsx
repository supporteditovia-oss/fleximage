import { LandingVoicePlayer } from "@/components/landing/LandingVoicePlayer";

/** Mini lecteur démo voix IA — widget landing uniquement. */
export function LandingVoiceDemo() {
  return <LandingVoicePlayer variant="widget" />;
}
