/** Tarifs Voix IA — partagés API (éviter 8 cr. qui bloque les petits soldes). */

const VOICE_GENERATE_CREDIT_COST = 5;
const VOICE_CLONE_CREDIT_COST = 10;

module.exports = {
  VOICE_GENERATE_CREDIT_COST,
  VOICE_CLONE_CREDIT_COST,
};
